apiVersion: v1
kind: ConfigMap
metadata:
  name: gs-config
data:
  config.conf: |
    [Server]
    name = GameDataServer
    host = 0.0.0.0
    port = 8002

    [Cors]
    origins = ["*"]

    [Database]
    host = 10.0.0.59
    user = admin
    database = user_data
    pool_size = 10